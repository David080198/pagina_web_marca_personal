# Variables de entorno para desarrollo y producción
# Rename this file to .env and fill in your values

# Flask Configuration
SECRET_KEY=dev-secret-key-change-in-production
FLASK_ENV=production
FLASK_DEBUG=False

# Database (for local development)
DATABASE_URL=sqlite:///codexsoto.db

# Admin Credentials (initial setup)
ADMIN_EMAIL=admin@codexsoto.com
ADMIN_PASSWORD=admin123

# Email Configuration (SMTP) - REQUIRED for password reset functionality
# Gmail: smtp.gmail.com (use app password, not account password)
# Yahoo: smtp.mail.yahoo.com
# Outlook: smtp-mail.outlook.com
# Custom: your-smtp-server.com
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_DEFAULT_SENDER=your-email@gmail.com

# Optional: If using a different database host (default is 'db' for Docker)
# DATABASE_HOST=db
# DATABASE_NAME=codexsoto_db
# DATABASE_USER=codexsoto
